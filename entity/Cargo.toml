[package]
name = "entity"
version = "0.1.0"
edition = "2021"

[dependencies]
sea-orm = { version = "1.1.0", features = ["default", "with-json", "with-chrono"] }
serde = {version = "1.0.213", features = ["derive"]}
serde_json = {version = "1.0.132"}